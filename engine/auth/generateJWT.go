package auth

import (
	"errors"

	"inverse.so/graph/model"
)

func CreateJWTToken(input *model.CreateJWTTokenInput) (*model.JWTCreationResponse, error) {
	return nil, errors.New("jwt flow not created")
}
